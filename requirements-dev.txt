# Dev-only dependencies. Installed via:
#   pip install -r requirements.txt -r requirements-dev.txt
#
# The runtime deps in requirements.txt stay lean so prod containers
# don't ship test machinery, fixtures, or developer tooling.

-r requirements.txt

pytest==8.3.4

# Linter — config lives in ruff.toml at the project root. Pinned
# so CI and local always agree on what counts as a lint error.
ruff==0.15.12
